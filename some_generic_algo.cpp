// min(a,b), max(a,b) + Comparators
// max_element(it1,it2), min_element(it1,it2) + Comparators --> returns iterator
// Comparators (<) for max_element() also <, min->false->swap, max->true->swap

// adjacent_difference(it1, it2, OutputItr)  +  Binary operation --> taken 2nd ele as first argument, first ele as 2nd argument
// adjacent_find(it1, it2) +  Binary predicate (default ==)  -->  it returns the iterator to first occurence of two consecutive ele that match, else if not iterator to last
